#These are the functijon that can be usable.
meats = (
    "beef",
    "pork",
    "chiken",
    "fish"
)

print(dir(meats)) #You can use the dir function.
print(help(meats)) #You can use the help fuction.
print(len(meats)) #You can use the len function also.
